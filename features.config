# EDIT TO CHANGE MUSL FEATURES

SUPPORTS_MMAP = 1
SUPPORTS_LOCALE = 0
SUPPORTS_RLIM = 0

ifeq ($(SUPPORTS_MMAP), 1)
CFLAGS += -DSUPPORTS_MMAP
endif

ifeq ($(SUPPORTS_RLIM), 1)
CFLAGS += -DSUPPORTS_RLIM
endif

ifeq ($(SUPPORTS_LOCALE), 1)
CFLAGS += -DSUPPORTS_LOCALE
endif
